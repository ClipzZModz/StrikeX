StrikeX/
  app/
    app.js              # Express app entry
    bin/
      www               # HTTP server bootstrap
    routes/             # View + API routes
    views/              # EJS templates
    public/             # Static assets
    storage/            # DB helpers + initialization SQL
      database.js
      initialization.sql
    middleware/
    utils/
  docs/
    business-plan.pdf
  project_theme/        # Local theme source (gitignored)
  old_strikex/          # Legacy project reference (gitignored)
  .env.example
  CHANGELOG.md
  README.md
  package.json
